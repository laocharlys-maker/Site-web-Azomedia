# Logo AzoMedIA

Dépose ici le fichier logo officiel (idéalement `logo.svg`, ou à défaut `logo.png` en haute résolution avec fond transparent).

Ce fichier sera utilisé dans `Header.astro` et `Footer.astro`.
